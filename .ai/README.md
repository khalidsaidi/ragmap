# .ai

Agent workspace for RAGMap.

Keep non-source artifacts here (notes, plans, run outputs). Large or transient artifacts must not be committed.

- `.ai/runs/**`: run logs and outputs (gitignored)
- `.ai/cache/**`: caches (gitignored)
- `.ai/tmp/**`: scratch (gitignored)

